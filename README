This program does Molecule Mechanics, solves the Poisson Bolzmann equation, and calculates surface area.

This project is available from a git directory, whose location is TBA. To obtain the latest version, run
git clone git://TBA

Then a development log may be found by running
git log (which is periodically dumped into ChangeLog)
